#!/usr/bin/env zsh

# Dev Shortcuts
alias c="code"
alias c.="code ."
alias .="code ."

alias bump="nu && cam 'Bump versions' && push"

# Node Shortcuts
alias y="ni"
alias r="nr "
alias rx="nlx"

# Now Shortcuts
alias deploy="vercel --prod"
